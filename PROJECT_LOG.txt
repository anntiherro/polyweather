Real-Time Prediction Market Accuracy Monitoring — Project Log
=============================================================
Date: June 3, 2026
Stack: Python, Apache Kafka, Apache Flink (Java), Docker, Open-Meteo API, Gamma API (Polymarket)


ЦЕЛЬ ПРОЕКТА
------------
Построить streaming pipeline который в реальном времени сравнивает ставки на погоду
с Polymarket с реальными данными погоды (Open-Meteo), чтобы находить неточные рынки
и арбитражные возможности для трейдеров.

Пример: маркет "Will it rain in Warsaw today?" торгуется по Yes=30%, но Open-Meteo
показывает дождь прямо сейчас — рынок недооценён, это торговая возможность.


ИНФРАСТРУКТУРА
--------------
Docker Compose поднимает:
- Kafka (confluentinc/cp-kafka, KRaft mode, без Zookeeper)
- Flink JobManager + TaskManager (apache/flink:latest = 2.2.1)
- kafka-init: создаёт 5 топиков при старте
- Kafka UI (provectuslabs/kafka-ui) на localhost:8080
- Flink UI на localhost:8081

Топики:
- polymarket-predictions-raw (3 партиции, retention 7 дней)
- weather-actuals-raw (3 партиции, retention 7 дней)
- market-weather-correlations (3 партиции, retention 30 дней)
- market-accuracy-aggregates (3 партиции, retention 30 дней)
- arbitrage-alerts (3 партиции, retention 30 дней)

Kafka настроена со Snappy компрессией — потребовала установки python-snappy
на хосте для Python producers.


КОМПОНЕНТ 1: POLYMARKET PRODUCER
---------------------------------
Файл: prediction-market-monitor/producers/polymarket_producer.py

Изначально использовал CLOB API (clob.polymarket.com) — не поддерживал нужную
фильтрацию. Переписан под Gamma API (gamma-api.polymarket.com/events) с тегом
"daily-temperature".

Ключевые решения:
- Фильтр по end_date = сегодня (только актуальные маркеты, ~627 штук)
- In-memory кеш цен (_price_cache): шлём в Kafka только если цена изменилась
  с прошлого poll — избегаем дублирования 600+ записей каждые 5 минут
- Парсинг города из текста вопроса (regex: "in <City>", "temperature in <City>")
- Экспоненциальный backoff для API ретраев (1→16 сек)
- POLL_TIMESTAMP в формате ISO 8601 без микросекунд ("%Y-%m-%dT%H:%M:%SZ")


КОМПОНЕНТ 2: WEATHER PRODUCER
------------------------------
Файл: prediction-market-monitor/producers/weather_producer.py

Изначально был написан с хардкодом городов и конечным циклом (LIMIT=10).
Полностью переписан:

- Динамически читает активные города из топика polymarket-predictions-raw (Kafka consumer)
  вместо повторных вызовов Gamma API — логичная декомпозиция, weather producer
  не должен знать про Polymarket API
- Геокодирование через geopy/Nominatim с in-memory кешем (1 запрос/сек rate limit)
- Запрашивает текущую погоду (current) у Open-Meteo для каждого города
- Бесконечный цикл, poll каждые 15 минут
- Структура записи соответствует спеке: current, current_units, LOCATION_NAME, POLL_TIMESTAMP

Зависимости: geopy, python-snappy (для Snappy декомпрессии Kafka)


КОМПОНЕНТ 3: FLINK CORRELATION JOB
------------------------------------
Файл: prediction-market-monitor/flink/jobs/src/main/java/com/polyweather/CorrelationJob.java
Build: Maven (pom.xml), собирается в fat JAR через maven-shade-plugin

Логика:
- Читает оба топика из Kafka (latest offset — только новые сообщения)
- Ключует оба потока по LOCATION_NAME
- CoFlatMapFunction: weather обновляет state, polymarket джойнит с последней погодой
- Для каждой пары считает: target_temp из вопроса (regex), actual_temp из погоды,
  ACTUAL_OUTCOME (exact/above/below match), PREDICTION_ERROR, CORRELATION_LATENCY_SEC
- Пишет результат в market-weather-correlations

Пример выходной записи:
  "Will the lowest temperature in Tokyo be 18°C on June 3?"
  yes_price=0.037, actual_temp=19.1°C → ACTUAL_OUTCOME=0, PREDICTION_ERROR=0.037


ПРОБЛЕМЫ И РЕШЕНИЯ
-------------------

1. Docker не запускался — Docker Desktop был закрыт. Решение: запустить вручную.

2. kafka-init падал раньше чем Kafka готова — добавлен wait-loop:
   until kafka-topics --list > /dev/null; do sleep 2; done

3. Python kafka consumer не мог читать Snappy-сжатые сообщения:
   UnsupportedCodecError. Решение: pip install python-snappy

4. Flink job: UnsupportedClassVersionError — скомпилировали под Java 21,
   Flink работает на Java 17. Решение: maven.compiler.source/target=17

5. Flink job: NoClassDefFoundError TwoPhaseCommittingSink — версия
   flink-connector-kafka 3.4.0-1.20 не совместима с Flink 2.2.1.
   Решение: обновить до 4.0.0-2.0

6. Flink job: open(Configuration) — в Flink 2.x API изменился,
   нужен open(OpenContext). Решение: исправить сигнатуру метода.

7. Flink job: NoSuchMethodError getNumberTypeFP — конфликт версий Jackson.
   Flink 2.2.1 бандлит Jackson 2.15.3, Kafka connector требует 2.16+.
   Решение: добавить jackson-core/databind 2.17.3 в fat JAR + использовать
   flink-shaded-jackson для компиляции.

8. DateTimeParseException: Python datetime.isoformat() генерирует
   "2026-06-03T09:45:05.928630+00:00" с микросекундами и timezone offset,
   Java Instant.parse ожидает чистый ISO формат.
   Решение: strftime("%Y-%m-%dT%H:%M:%SZ") в обоих продьюсерах.

9. Flink job читал с earliest offset — обрабатывал все 600+ polymarket записей
   до того как weather попадал в state → correlations топик оставался пустым.
   Решение: переключить на latest offset (только новые сообщения).

10. Концептуальная проблема: большинство маркетов Polymarket сегодня —
    "highest/lowest temperature of the day". Сравнение с текущей температурой
    некорректно до конца дня. Обсуждены варианты:
    - Использовать winner field (когда маркет закрылся)
    - Запрашивать daily forecast вместо current погоды
    Решение для MVP: оставить current погоду, признать что ACTUAL_OUTCOME
    для daily маркетов промежуточный до конца дня.


ТЕКУЩИЙ СТАТУС (на конец дня)
------------------------------
Готово (MVP частично):
✓ Инфраструктура Docker Compose (Kafka + Flink + UI)
✓ Polymarket producer (фильтрация по дате, delta-only отправка)
✓ Weather producer (динамические города из Kafka)
✓ Flink Correlation Job (RUNNING, пишет в market-weather-correlations)
✓ End-to-end pipeline работает: данные текут через все 3 компонента

Не сделано (следующие сессии):
- Компонент 4: Accuracy Aggregation (sliding windows, rolling metrics)
- Компонент 5: Anomaly Detection + Arbitrage Alerts
- Компонент 6: Data Quality Layer
- Компонент 7: Dashboard (Grafana / Streamlit)
- PostgreSQL для хранения агрегатов
- Weather producer: переход на daily forecast для корректного сравнения
- requirements.txt (удалён, нужно восстановить)


ЗАВИСИМОСТИ ДЛЯ ЗАПУСКА
------------------------
Python (хост):
  pip install kafka-python requests geopy python-snappy

Java/Maven:
  JDK 17+ (установлен: Microsoft OpenJDK 21)
  Maven 3.9+ (установлен: ~/.maven/apache-maven-3.9.16)

Docker: все сервисы через docker compose up -d


CHOSEN STRATEGY: HYBRID WEATHER DATA (Variant C)
-------------------------------------------------
Decided: June 11, 2026

The core problem (issue #10) is that most Polymarket markets ask about daily
high/low temperatures ("Will the highest temp in Tokyo be 18°C today?"), making
it incorrect to compare against the current real-time temperature until end of day.

Chosen approach — hybrid data sourcing based on market status:

1. OPEN markets (active, no winner yet):
   - Fetch daily forecast from Open-Meteo Forecast API (temperature_2m_max /
     temperature_2m_min). This gives an intra-day directional signal — not ground
     truth, but useful for detecting obvious mispricings while the market is live.

2. CLOSED markets (resolved, winner field is set):
   - Fetch actual historical data from Open-Meteo Archive API for the market's
     end date. Compare the real daily max/min against the market's resolved outcome
     (winner = Yes/No) to build a ground-truth accuracy record.

Why this matters:
   Closed markets + archive data provide the ground truth needed to measure how
   well Polymarket prices reflect reality over time. Accumulating this accuracy
   history per city and question type enables detection of systematically
   mispriced markets — the core trading signal the pipeline is designed to produce.

Implementation plan:
   - Weather producer: add second fetch mode for daily forecast (max/min) and
     archive lookups for resolved markets
   - Flink Correlation Job: add branch for closed-market processing using winner
     field and historical weather
   - Component 4 (Accuracy Aggregation): rolling accuracy metrics per city,
     powered by the closed-market ground truth


СЕССИЯ 2 — June 11, 2026
--------------------------

РЕАЛИЗАЦИЯ СТРАТЕГИИ C (Hybrid Weather Data)

1. polymarket_producer.py
   - Добавлен fetch_closed_markets() — забирает вчерашние закрытые маркеты
     из Gamma API (closed=true, end_date=yesterday)
   - Исправлено извлечение поля winner из market.winner API
   - Закрытые маркеты не кешируются в _price_cache (иначе цены 0.0/1.0
     никогда не меняются и маркеты не переотправляются)

2. weather_producer.py
   - Переход с одного fetch_weather() на два режима:
     fetch_forecast() — current + daily max/min для открытых маркетов
     fetch_historical() — archive-api.open-meteo.com для закрытых
   - get_market_locations() возвращает (open_cities, closed_locations)
   - Записи включают data_type: "forecast" | "historical"
   - SSL-проблема на Windows (UNEXPECTED_EOF): Python OpenSSL несовместим
     с корпоративной сетью; решено через PowerShell subprocess с
     -EncodedCommand (использует Windows Schannel)

3. CorrelationJob.java
   - Два отдельных ValueState: latestForecastWeather, latestHistoricalWeather
   - flatMap2 маршрутизирует по data_type: historical → historicalState
   - flatMap1 для closed=true читает из latestHistoricalWeather
   - detectQuestionType(): HIGHEST / LOWEST / EXACT по тексту вопроса
   - Для HIGHEST берёт temperature_2m_max, для LOWEST — temperature_2m_min
   - Для закрытых маркетов: winner field как ground truth (fallback на
     temperature comparison если winner=null)
   - Новые поля в выходной записи: question_type, data_type, market_closed
   - Offset weather source переключён на earliest (новый group ID
     correlation-job-weather-v2 чтобы сбросить committed offsets)

4. AccuracyJob.java (новый, Компонент 4)
   - Читает из market-weather-correlations
   - KeyBy LOCATION_NAME, накапливает rolling stats через ValueState
   - Метрики: total_count, correct_count, accuracy_rate, avg_prediction_error,
     max_prediction_error + отдельно closed_* для ground truth метрик
   - Пишет в market-accuracy-aggregates после каждой входящей записи

ИНФРАСТРУКТУРНЫЕ ИЗМЕНЕНИЯ
- docker-compose.yml: taskmanager.numberOfTaskSlots: 2 → 4,
  taskmanager.memory.process.size: 2048m (для двух параллельных джобов)
- Python зависимости добавлены: kafka-python-ng (fix six.moves на Python 3.14),
  httpx (SSL fallback), geopy

ПРОБЛЕМЫ И РЕШЕНИЯ

11. SSL UNEXPECTED_EOF при запросах к Open-Meteo из Python: Python OpenSSL
    не проходит через корпоративную TLS-инспекцию. PowerShell (Windows
    Schannel) работает. Решение: ps_get() через subprocess + EncodedCommand.

12. kafka-python six.moves ImportError на Python 3.14: kafka-python не
    поддерживает Python 3.12+. Решение: pip install kafka-python-ng.

13. CorrelationJob читал weather с latest offset (committed group offsets
    блокировали earliest): исторические записи оставались за committed
    offset. Решение: новый group ID сбрасывает committed offsets.

14. Debug-код вызывал m.find() дважды на одном Matcher — второй вызов
    возвращал false → early return, корреляции не эмитились.

15. market-accuracy-aggregates создан с cleanup.policy=compact, но
    Flink KafkaSink отправляет без ключа → Kafka отклоняет. Решение:
    пересоздать топик без compact.

ТЕКУЩИЙ СТАТУС (конец сессии 2)
---------------------------------
✓ Стратегия C полностью реализована
✓ Open market correlations: data_type=forecast, ~3700+ записей
✓ Closed market correlations: data_type=historical, ~3800+ записей
✓ AccuracyJob написан и скомпилирован в fat JAR
✗ AccuracyJob не удалось стабилизировать (проблемы с compact topic,
  OOM TaskManager при 2 слотах, требует 4 слота + 2GB)

Следующая сессия:
- Убедиться что AccuracyJob RUNNING стабильно
- Компонент 5: Anomaly Detection + Arbitrage Alerts
- Компонент 7: Dashboard (Grafana или Streamlit)


СЕССИЯ 3 — June 11, 2026
--------------------------

ВЕРИФИКАЦИЯ ПАЙПЛАЙНА + ФИКС °F/°C

Проведена сквозная проверка работающего стека против спецификации.

СТАТУС ИНФРАСТРУКТУРЫ
- Kafka + kafka-ui + kafka-init: подняты, все 5 топиков создаются корректно
- Flink JobManager + TaskManager (4 слота, 2GB): RUNNING
- CorrelationJob + AccuracyJob: оба RUNNING, задеплоены из fat JAR
- Python producers: запускаются вручную из venv (polyweather/.venv)

ДАННЫЕ В ТОПИКАХ (верифицировано)
- polymarket-predictions-raw: реальные маркеты на сегодня (NYC, Tokyo, London и др.)
- weather-actuals-raw: 50+ городов, forecast + historical записи
- market-weather-correlations: корреляции с ACTUAL_OUTCOME и PREDICTION_ERROR
- market-accuracy-aggregates: пусто (AccuracyJob не получал входных данных)

КРИТИЧЕСКИЙ БАГ: TEMP_PATTERN не матчил °F маркеты
Все американские маркеты (NYC и др.) используют °F в вопросах:
  "Will the lowest temperature in New York City be between 70-71°F on June 11?"
Старый паттерн: `(\d+(?:\.\d+)?)\s*°?C` — требует C в конце, °F вопросы
тихо дропались (m.find() → false → early return без эмита корреляции).

Решение в CorrelationJob.java:
1. Паттерн `°?C` → `°?([CF])` — матчит оба юнита, захватывает группу
2. Конвертация °F → °C: if ("F".equals(m.group(2))) targetTemp = (t - 32) * 5.0 / 9.0

Для диапазона "70-71°F" паттерн захватывает 71 (последнее число перед юнитом)
как верхнюю границу диапазона — достаточно для ±1°C tolerance в computeOutcome.

ВЕРИФИКАЦИЯ ФИКСА
После ребилда JAR (mvn package) и ресабмита джобов в Flink:
  "Will the highest temperature in New York City be 71°F or below on June 10?"
  target_temp: 21.67°C (= 71°F конвертировано), actual_temp: 29.2°C
  ACTUAL_OUTCOME: 0, market_closed: true — корректно ✓

NYC и другие °F маркеты теперь присутствуют в market-weather-correlations.

ПРОЧИЕ НАХОДКИ (не исправлены в этой сессии)
- current_units.temperature_2m = "Â°C" для части городов (Amsterdam и др.) —
  encoding glitch при сериализации °C символа; cosmetic, на логику не влияет
- AccuracyJob не имеет sliding windows как в спеке (1h/15min slide) —
  сейчас per-event аккумуляция бесконечным state
- Отсутствуют: Component 5 (AnomalyJob), PostgreSQL, Prometheus, Grafana,
  Schema Registry, Flink restart strategy

ТЕКУЩИЙ СТАТУС (конец сессии 3)
---------------------------------
✓ °F/°C баг исправлен и верифицирован в проде
✓ Все компоненты MVP работают end-to-end (Kafka → producers → Flink → topics)
✓ NYC + US маркеты теперь корректно коррелируют

Следующая сессия:
- Компонент 5: AnomalyJob (→ arbitrage-alerts топик)
- PostgreSQL + Grafana dashboard
- Flink restart strategy в docker-compose


СЕССИЯ 4 — June 12, 2026
--------------------------

КОМПОНЕНТ 5: ANOMALY DETECTION JOB

Написан AnomalyJob.java — последнее недостающее Flink звено в цепочке.

Два входных потока:
- market-accuracy-aggregates → accuracy anomaly detection per city
- market-weather-correlations → arbitrage detection per market

Четыре типа алертов в arbitrage-alerts:
- ACCURACY_DROP    — accuracy упала >20% vs предыдущее окно для города
- ACCURACY_ANOMALY — accuracy отклонилась >2σ от running baseline
                     (Welford online algorithm для mean/variance)
- ARBITRAGE        — price_sum != 1.0 (флаг от upstream producer)
- HIGH_ERROR       — PREDICTION_ERROR > 0.70 на одном маркете

Алерты сразу появились на реальных данных:
  Chengdu: prediction_error=0.999 (маркет на 31°C при actual=31.1°C торговался по 0.001)
  Hong Kong: prediction_error=0.855
  London: prediction_error=0.705

РЕФАКТОР AccuracyJob — SLIDING WINDOWS

Переписан AccuracyJob с правильными sliding windows как в спеке
(было: per-event аккумуляция → тысячи дублей):

- SlidingProcessingTimeWindows(1 час, 15 мин slide)
- Эмитит 1 агрегат на город каждые 15 минут, не на каждое сообщение
- Добавлен bias_score = mean(yes_price - actual_outcome) для детекции
  систематической переоценки/недооценки рынка
- Добавлены window_start / window_end в выходную запись

СКРИПТ ЗАПУСКА start.ps1

Создан start.ps1 в корне проекта — поднимает весь стек одной командой:
1. docker compose up -d
2. Ждёт готовности Flink (polling /overview)
3. mvn package (пересборка JAR если код менялся)
4. Загружает JAR в Flink REST API
5. Сабмитит все 3 джоба (Correlation, Accuracy, Anomaly)
6. Открывает оба продюсера в отдельных окнах

Запуск: .\start.ps1 из корня проекта

ОЧИСТКА ТОПИКОВ

Все 5 топиков очищены от исторических данных (разные дни, >40k сообщений)
через kafka-delete-records до latest offset. Топики сохранены, только
данные удалены. Теперь пайплайн работает только с данными за сегодня.

ТЕКУЩИЙ СТАТУС (сессия 4 в процессе)
--------------------------------------
✓ AnomalyJob написан, RUNNING, пишет реальные алерты
✓ AccuracyJob переписан со sliding windows (1h/15min)
✓ start.ps1 — полный автозапуск стека
✓ Все топики очищены, пайплайн работает на свежих данных
✓ Все 3 Flink джоба RUNNING

В процессе:
- PostgreSQL контейнер + схема
- Grafana dashboard
- Flink restart strategy в docker-compose
